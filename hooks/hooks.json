{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "{ [ -f \"${CLAUDE_PLUGIN_ROOT}/hooks/always-on.sh\" ] && sh \"${CLAUDE_PLUGIN_ROOT}/hooks/always-on.sh\"; } || true",
            "timeout": 30,
            "statusMessage": "Checking i-have-adhd always-on flag..."
          }
        ]
      }
    ]
  }
}
